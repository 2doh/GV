export interface boardStateInterface {
  cursorState: string;
  setCursorState: (cursorState: string) => void;
}

export interface boardColorStateInterface {
  colorState: string;
  setColorState: (color: string) => void;
}
